# ISSUE-13: Add one worked normalized-query example

## What to do

- Show one example of how a reported search strategy becomes a normalized Boolean query.
- Make the simplification process concrete.

## How to do it

- Choose a source strategy that is representative and manuscript-friendly.
- Show source form, normalized form, and the main transformations applied.
- Place the example in `SCIDATA_26_SR4ALL/scidata--sr4all-part5.tex`.

## Definition of done

- `Usage Notes` contains one query-normalization example.
- The example makes the approximation nature of the normalized query explicit.
- A reviewer can see what is retained and what is dropped.
